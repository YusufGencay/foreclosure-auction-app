# Florida Foreclosure Auction Analysis & Ranking Tool — Project Context

## What this is
A decision-support web app for a real estate investor (Yusuf) who bids at Florida foreclosure auctions. It aggregates upcoming foreclosure auction listings from FL county clerk sites, scores/ranks properties against investment criteria, and provides a single dashboard to triage properties quickly.

## Geographic scope (config-driven, in `config/counties.yaml`)
- Tampa Bay: Hillsborough, Pinellas, Pasco, Hernando, Manatee, Sarasota
- Central FL: Orange, Osceola, Seminole, Polk, Lake, Volusia, Brevard, Marion
- CORRECTED 2026-07-05 (per user, confirmed live via browser): 12/14 counties run active mortgage-foreclosure calendars on RealAuction (realforeclose.com). Seminole IS one of them (seminole.realforeclose.com - live, real scheduled sales) - an earlier version of this doc wrongly claimed Seminole used a custom webapps.seminoleclerk.org system; that was never actually verified live and has been corrected. Lake and Osceola are the only 2 exceptions: their realforeclose.com foreclosure calendars are confirmed offline ("This feature is currently offline"); both instead run active tax deed sales on realtaxdeed.com, which is what config/counties.yaml now points at for those two.

## Tech stack
Python FastAPI backend + React/Vite frontend + SQLite DB. APScheduler for daily scrape scheduling. Single-service Docker deploy (FastAPI serves the built React app as static files under one process, API under `/api/*`).

## Where the code lives
Local folder: `foreclosure-app/` inside the Cowork outputs folder (persists on Yusuf's Mac). Contains `backend/`, `frontend/`, `config/`, `Dockerfile`, `railway.toml`, `.env.example`, `README.md`.

## Build status — what's real vs. stubbed (be honest about this, never claim more than what's true)
- **Scoring engine**: fully implemented — equity spread ($200k threshold, always shows $ spread), lien priority flag (heavy penalty for HOA/COA or junior-lien foreclosures where a senior mortgage survives), taxes owed / code liens / HOA balance (proportional penalties), bankruptcy flag. Weights are DB-backed and adjustable via `GET/PUT /api/weights`.
- **Absorption rate & flood zone**: honest placeholders — no free reliable source wired in yet, clearly labeled "unavailable," never fabricated.
- **Crime rate**: wired to FBI Crime Data API, pending an API key.
- **Title search**: swappable stub — returns "not_configured" until a real provider key is added server-side (never client-side).
- **Scrapers**: framework is real (rate limiting, retries, per-county failure isolation so one bad county doesn't crash the batch, logs to `scrape_logs` table). UPDATED 2026-07-05: `backend/scrapers/realauction_playwright.py` is now built - a Playwright/headless-Chromium adapter covering all 13 RealAuction-platform counties (both realforeclose.com and realtaxdeed.com use the same underlying template). Written directly from a real, live-inspected DOM structure (div.AUCTION_ITEM / table.ad_tab), confirmed via a real browser session that the listing data loads through an internal AJAX handshake only a real rendered page can complete (a bare HTTP fetch replay gets nothing back - confirming the original JS-rendering concern). Extracts case number, final judgment/opening bid, parcel ID, address, assessed value, sale date/time; leaves everything not shown on the listing page (plaintiff, liens, taxes, HOA, bankruptcy, flood zone, market value/comps) honestly null. `main.py` now also upserts scraped records into the Property table (previously scrapes ran and logged but never wrote results - that gap is fixed). Covered by a real unit test suite (`backend/tests/test_realauction_playwright.py`, passes) using live-captured field values. NOT YET live-verified end-to-end: this dev sandbox has neither the root access to install Chromium's OS deps nor network egress to realforeclose.com, so the actual page.goto()-driven scrape has not been run against the real internet yet - that's the next milestone, ideally once deployed (Railway has neither restriction). The app still also seeds a clearly-tagged fictional demo dataset (`is_demo_data=true`, `SAMPLE_DEMO_DATA_NOT_REAL`) as a fallback so the UI/scoring/filtering has something to show before a county's first real scrape lands.
- **Phase 1 (2026-07-05): on-demand third-party estimate scrapers.** Added `zillow_estimate`, `realtor_estimate`, `redfin_estimate` (Float), `market_conditions` (String), `ranking_score` (Float), `estimates_last_updated` (DateTime) to the `Property` model. New Playwright-based scrapers: `backend/scrapers/zillow_scraper.py` (`get_zillow_estimate`), `backend/scrapers/realtor_scraper.py` (`get_realtor_estimate`), `backend/scrapers/redfin_scraper.py` (`get_redfin_estimate`), `backend/scrapers/market_conditions.py` (`get_market_conditions` → Redfin's per-zip "seller's/buyer's market" classification sentence, the only free source identified for this signal). Shared honesty/rate-limit/timeout logic lives in `backend/scrapers/estimate_common.py` (30s page timeout, 3s min delay between requests, regex-based dollar-figure extraction near each site's known estimate label, a bot-block/CAPTCHA heuristic, never a fabricated number). New endpoint `GET /api/properties/{id}/enrich` calls all four lookups, updates the Property record, and returns the enriched property with an `enrich_errors` list (per-scraper failures are isolated so one blocked site never prevents the other three from updating) and `enrich_cached` flag; skips re-scraping if `estimates_last_updated` is under 24h old. `ranking_score` was a placeholder mirroring `composite_score` at the time (superseded by the real ranking formula in Phase 2, see below). Also added `db.ensure_columns()`, a small additive auto-migration run at startup (this project has no Alembic) so an existing `data/foreclosure.db` from before this change picks up the new columns via `ALTER TABLE ... ADD COLUMN` instead of throwing "no such column" errors - verified against a synthetic old-schema DB, existing rows/data untouched. Covered by `backend/tests/test_estimate_scrapers.py` (21/21 backend tests pass). Like the RealAuction scraper, the Zillow/Realtor.com/Redfin fetches are NOT live-verified against the real sites from this sandbox (no network egress, no Chromium binaries installed here) - confirmed the failure path is graceful (returns `None`, `enrich_errors` stays empty since missing-browser is caught internally) rather than crashing. Real end-to-end verification is the next milestone once this runs somewhere with real network + Chromium access (e.g. after Railway deploy).
- **Phase 2 (2026-07-05): 0-100 ranking formula + twice-daily auto-scrape.** `backend/scoring.py` now has `compute_ranking_score(property_row, weights) -> float` (0-100): 50% deal quality (`gap = (avg_of_available_estimates - final_judgment) / avg_of_available_estimates`, clamped to ±100% and rescaled to 0-100 - bigger gap/more built-in equity scores higher) + 50% risk (weighted-average of the existing scoring engine's lien_priority/bankruptcy/taxes_owed/code_liens/flood_zone/crime_rate component scores, rescaled to 0-100 where higher = lower risk). If no third-party estimate is available yet (enrich hasn't run, or all three scrapers came back null), deal quality is dropped entirely and the rank is the risk score alone, per spec, rather than guessing. `main._rescore_all()` and the `/enrich` endpoint both now store this real value in `Property.ranking_score`, and `GET /api/properties` defaults `sort_by` to `ranking_score DESC` (still overridable via the existing `sort_by`/`sort_dir` params). Added `Property.auction_status` ("active"/"canceled" - distinct from the investor's own `flag_status` saved/dismissed workflow) and `ScrapeLog.new_count`/`.canceled_count`. The scheduler now registers two cron jobs, `scrape_all_6am` (`0 6 * * *`) and `scrape_all_6pm` (`0 18 * * *`), both running the renamed `main.scrape_all_counties()` (previously a single 03:00 job) - registration is split into `main._register_scheduler_jobs()` so it's independently testable. Every scrape now diffs the freshly-scraped case numbers against existing non-demo Property rows for that county: anything missing that hasn't already happened (sale_date in the future or unset) gets marked `auction_status="canceled"` rather than silently deleted or left stale; a case number reappearing later flips it back to "active". Per-county failure isolation is preserved (one county's exception, e.g. a bad scraper adapter or a network error, never aborts the batch or the scheduled job). New tests: `backend/tests/test_ranking.py` (10 hand-verified scenarios - isolated-risk-weight and isolated-deal-quality constructions so expected values are computable by hand, not just re-deriving the function under test), `backend/tests/test_scrapers.py` (19 cases mocking Playwright itself for zillow/realtor/redfin/market_conditions success, no-estimate-found, CAPTCHA-block, and navigation-failure paths), `backend/tests/test_scheduler.py` (7 cases: cron registration/timing/idempotency, plus `scrape_all_counties()` iterating every county and isolating one county's simulated crash). All 57 backend tests pass. Manually verified end-to-end against an isolated in-memory DB with a fake scraper across two simulated scrape passes: a dropped case number got marked canceled (not deleted), a new one was inserted, `ranking_score` correctly jumped once real estimates were attached, and `scrape_logs` recorded accurate new/canceled counts per run - this is the closest thing to a live-network verification possible in this sandbox (no real egress to realforeclose.com or the three estimate sites here, same limitation noted in every scraper module's docstring).
- **Frontend**: sortable/filterable dashboard table, focused 1-2-property card view toggle, property detail view with Zillow/Realtor.com deep links (built from address, not scraped), risk warning banners (HOA/junior-lien/flood/bankruptcy), notes/flag/dismiss controls, weight-adjustment sliders, CSV/XLSX export.
- **API endpoints**: properties list/detail/update, counties, scrape (per-county/all), scrape-status, weights, export, title-search stub.
- **Phase 3 (2026-07-05): calendar/list view + tracking features (frontend + backend).** New DB tables `Watchlist` (property_id unique, saved_at) and `BidRecord` (property_id, bid_amount, sale_price, winner, notes, created_at); new `Property.investor_notes` (Text) field kept deliberately separate from the pre-existing `notes` field (which the scraper also writes NOT_SCRAPED_NOTE into) so investor-entered notes never get clobbered by a re-scrape. New endpoints: `GET/POST/DELETE /api/watchlist[/{id}]`, `PATCH /api/properties/{id}/notes`, `POST/GET /api/bid-records`, `GET /api/export/csv?columns=...` (column-selectable export, kept alongside the pre-existing `/api/export` which always exports every column). `GET /api/properties` gained `filter=by_date&date=YYYY-MM-DD` (exact-day match on sale_date) and `watchlist_only=true`. `_property_to_dict` now also returns `is_watchlisted` and `days_to_auction` (computed from sale_date vs. today). New frontend components: `PropertyCard.jsx` (reusable rank/county/address/judgment/opening-bid/sale-date/days-to-auction card, used in both the dashboard's card view and the calendar's day expansion), `CalendarView.jsx` (month grid, loads the whole month's auction counts in one call, click a day to expand via the by_date endpoint), `WatchlistButton.jsx` (star icon, DB-backed via the new watchlist endpoints), `NotesPad.jsx` (investor_notes textarea, auto-saves on blur via the new PATCH endpoint), `PreBidChecklist.jsx` (modal checklist, intentionally localStorage-only per spec - not DB-backed), `CsvExportPanel.jsx` (column checkboxes feeding the new `/api/export/csv`). A new "Calendar" tab was added to the main nav in `App.jsx`; the dashboard's ad hoc card-view markup was replaced with the shared `PropertyCard`; a bid-history log (table + entry form) was added to `PropertyDetail.jsx`. All existing 57 backend tests still pass; manually smoke-tested every new endpoint against a test DB (watchlist add/list/remove, notes patch, bid record create/list, by_date filter, csv column export) - all returned 200 with correct data. Frontend builds cleanly (`npm run build`, 28 modules, no errors) - the one failure encountered was an `EPERM` deleting old files in the synced `dist/` folder (a filesystem quirk of this mounted directory, same class of issue `db.py` already works around for SQLite locking), not a code problem; confirmed by building to a scratch directory instead, which succeeded. Since the Dockerfile's frontend-build stage runs `npm run build` fresh inside the container (see Dockerfile stage 1), the stale local `frontend/dist/` in this sandbox doesn't affect the real deploy - Railway will rebuild it from source on push. Components were kept flat under `frontend/src/` (matching this project's existing convention - `CountyPanel.jsx`, `PropertyDetail.jsx`, etc. are not nested under a `components/` folder) rather than under `frontend/src/components/` as a literal spec path might suggest. NOT YET pushed to GitHub/Railway as of this writing - next step is the browser-based upload flow described below.

## Railway deployment status — LIVE (updated 2026-07-05)
- **Deployed and live**: https://foreclosure-auction-app-production.up.railway.app, in Railway project **marvelous-benevolence**, service **foreclosure-auction-app**. GitHub repo: `YusufGencay/foreclosure-auction-app` (public), connected to Railway for auto-deploy on every push to `main`. A Volume (`foreclosure-auction-app-volume`) is mounted for the SQLite DB, and `DB_PATH`/`FEMA_API_BASE_URL` are set as service variables. This was already set up in a prior session (not reflected in this doc until now, since this doc had gone stale) — the "nothing pushed yet" note below is now obsolete.
- The Phase 1 + Phase 2 backend changes (estimate scrapers, `compute_ranking_score`, twice-daily scheduler, `db.ensure_columns()` auto-migration) were pushed to the repo via GitHub's web upload flow (`Add file > Upload files`, same pattern as prior commits in this repo's history) using the Claude-in-Chrome browser extension, since this Cowork sandbox has no network route to github.com/railway.app. Railway's GitHub integration auto-deployed on push; deploy logs confirmed a clean startup (`Scheduler started: scrape-all jobs registered for 06:00 and 18:00.`).
- **Real-world confirmation, not sandbox-only anymore**: the live deployment already has 192 real (non-demo) Hillsborough County properties scraped from `hillsborough.realforeclose.com` via `realauction_playwright.py` — the live end-to-end verification that was the top item on the "next real engineering work" list is done. The other 13 counties haven't had their first scrape yet (`last_scraped_at: null` for all of them as of this check) — that'll happen at the next 06:00/18:00 scheduled run, or can be triggered now via `POST /api/scrape/all`.
- **Real-world test of `/enrich`**: called `GET /api/properties/10/enrich` against the live deployment (real Chromium via Playwright, real network). All three lookups actually ran (no "playwright not installed" error — the Docker image's `playwright install --with-deps chromium` step worked) but all returned `None`: Zillow and Redfin both hit `Page.goto: Timeout 30000ms exceeded` waiting for `networkidle`, and Realtor.com loaded successfully but found no "Realtor.com Estimate" label on its search-results page (an address query there returns a listings page, not a direct property match). `enrich_errors` was empty (no exceptions - exactly the intended graceful-failure behavior) and no data was fabricated. **Likely root cause worth fixing next**: these estimate scrapers wait for Playwright's `wait_until="networkidle"`, but Zillow/Redfin's real pages apparently never go fully network-idle (continuous analytics/polling traffic), so the 30s timeout fires before the page even finishes rendering - this may be a `networkidle`-vs-`domcontentloaded` wait-strategy issue rather than (or in addition to) bot-blocking, since no CAPTCHA/block text was ever detected in the (empty, because it never loaded) page content. Next step: try `wait_until="domcontentloaded"` plus a short explicit wait or a selector-based wait for a known price element, and re-test live.
- Un-set / not yet configured: `TITLE_SEARCH_API_KEY`, `TITLE_SEARCH_PROVIDER`, `CRIME_DATA_API_KEY` — intentionally left blank until Yusuf has real provider credentials, per the "never fabricate" guardrail.

## Comprehensive live bug sweep (2026-07-05, evening) — root causes found and fixed
User reported broad data-quality complaints ("almost everything is not working"). Did a systematic live-DOM/live-network investigation (real Chrome browser, not this sandbox) rather than only checking the specific items raised, and found and fixed five distinct real bugs, all pushed live and confirmed deployed:

1. **`realauction_playwright.py` was silently dropping most of a day's auctions and mixing in closed/canceled ones.** Live DOM inspection of hillsborough.realforeclose.com showed `.AUCTION_ITEM` matches items in both `#Area_W` (real upcoming auctions) and `#Area_C` (already-closed/canceled) with no distinction, and the "Auctions Waiting" list paginates via an AJAX-in-place `.PageRight` control that the scraper never clicked — confirmed on a real 15-active-auction day that only 10 were being captured (page 1 only) and closed sales could leak in as if upcoming. Fixed: scoped extraction to `#Area_W`, tag+drop `#Area_C` items, added a bounded pagination loop keyed on `#curPWA`/`#maxWA` (capped at `MAX_PAGES_PER_DAY=20`), falls back to old single-page/unscoped behavior if that DOM structure isn't present.
2. **Zillow/Realtor.com/Redfin scrapers were guessing URLs that don't work.** Live-checked: `zillow.com/homes/<address>_rb/` and `zillow.com/homedetails/<address>/` (no zpid) both silently redirect to a generic rental search with zero relation to the requested address; Redfin's `/search?query=...` redirects to the homepage. Fixed: Zillow/Realtor.com now resolve the address to its real canonical detail-page URL (with the required internal zpid/listing-id) via a `site:<domain> <address>` query against `html.duckduckgo.com`'s no-JS HTML search endpoint, which reliably surfaces the correct URL as its top hit (confirmed live for a real address on both sites). Redfin uses its own `stingray/do/location-autocomplete` API first (also confirmed live) with the same DuckDuckGo approach as a fallback.
3. **Estimate scrapers were hanging on `wait_until="networkidle"`.** Confirmed live in production logs: both Zillow and Redfin never go fully network-idle (continuous background polling/analytics), so every real `/enrich` call timed out at 30s before the page even rendered — no CAPTCHA ever detected because the page never loaded. Fixed: switched to `wait_until="domcontentloaded"` + a short explicit settle wait, the standard fix for this class of issue.
4. **The dollar-figure extraction window was too tight.** Live-checked Realtor.com's real rendered page: the "RealEstimate" label and its dollar figure are ~90 characters apart once flattened to plain text (interleaved chart/table UI text), but the regex only tolerated 40 characters, so a real, present estimate would have been missed. Widened to 400 characters.
5. **The raw-JSON fetch path (Redfin's autocomplete API, the DuckDuckGo search) had a render race.** Confirmed live: `page.goto(url)` + `page.inner_text("body")` is meant for rendered HTML pages, but for a raw JSON API response Chromium renders its own JSON-viewer UI over it, and reading that back raced with the viewer in production (`json.JSONDecodeError: Extra data...` on a response that was the correct literal text when checked interactively). Fixed: switched to Playwright's dedicated API request context (`playwright.request`), which does a plain HTTP GET with no DOM/rendering step at all - faster and immune to this race.
6. **`POST /api/scrape/all` was 404ing in production the entire time** (found while investigating why 13/14 counties had never scraped). Root cause: `POST /api/scrape/{county}` was registered in `main.py` *before* `POST /api/scrape/all`, so FastAPI/Starlette (which matches path routes in registration order) matched `{county}="all"` first, looked up a county literally named "all", found none, and raised a 404 — logged as a bare "404 Not Found" with no indication this was a routing bug rather than an intentional not-found. This is the actual reason the other 13 counties never got their first scrape: the only thing that ever successfully ran `scrape_all_counties()` was the scheduled 06:00/18:00 APScheduler job, which calls that Python function directly rather than going through this HTTP route. Fixed by moving the exact-literal route above the parameterized one.

All 60 backend tests pass (test_scrapers.py's Playwright mocks updated to cover both the page-render path and the new request-context path). All six fixes pushed to `main` via GitHub web upload across four commits and confirmed "Deployment successful" on Railway for each. After the routing fix went live, triggered a real `POST /api/scrape/all` in production (fired via a live browser tab, not this sandbox) to scrape all 13 previously-unscraped counties for the first time — this is a long-running operation (13 counties × up to 45 lookahead days each, rate-limited) that was still in progress as of this writing; check `GET /api/scrape-status` for current per-county `last_scraped_at` to see completion, or wait for the next scheduled 06:00/18:00 run to pick up whatever it doesn't finish. Also live-verified every Phase 3 endpoint directly against production (watchlist add/list, notes patch, bid-record create/list, calendar by-date filter, CSV export) — all returned 200 with correct data.

## Fastest path to finish deployment (recommended over browser-automation file-by-file push)
1. On Yusuf's Mac, in the `foreclosure-app` folder: `git init && git add . && git commit -m "initial"` (confirm `venv/`, `node_modules/`, `frontend/dist/` are gitignored first).
2. Create a new GitHub repo, `git remote add origin <url> && git push -u origin main`.
3. In Railway → marvelous-benevolence project → Cmd+K → New Service → GitHub Repo → select the new repo.
4. Add a Volume mounted at `/data`.
5. Set env var `DB_PATH=/data/foreclosure.db`. Leave `TITLE_SEARCH_API_KEY` / `TITLE_SEARCH_PROVIDER` / crime data key unset until Yusuf has real providers — never fabricate keys.
6. Deploy, watch build logs, fix/iterate if the Docker build fails.

## Next real engineering work needed (in priority order)
1. ~~Live-verify the new `realauction_playwright.py` scraper end-to-end against the real internet~~ - DONE 2026-07-05: confirmed live in production, 192 real Hillsborough properties scraped successfully. Remaining: trigger/wait for the other 13 counties' first scrape (scheduled job or manual `POST /api/scrape/all`).
2. **Fix the Zillow/Redfin `/enrich` scrapers' wait strategy** - live-tested 2026-07-05 and both consistently hit `Page.goto` `networkidle` timeouts (30s) rather than returning real estimates or a detected CAPTCHA/block page. Try `wait_until="domcontentloaded"` + a short explicit/selector wait instead of `networkidle`, then re-test live. Realtor.com's scraper does load successfully but needs its search-URL/selector logic revisited so an address query lands on an actual property detail page instead of a results list.
3. ~~Build/verify a Seminole County adapter~~ - not needed; Seminole runs on RealAuction like 11 other counties (corrected 2026-07-05), covered by the same adapter.
4. Wire a real absorption-rate data source (or confirm none exists and leave as permanent placeholder).
5. Wire a real FEMA flood zone lookup (public API, no key needed — https://www.fema.gov/about/openfema/api).
6. Pick and wire a real title-search/lien-data API provider once Yusuf selects one and supplies a key (server-side env var only).

## Guardrails to keep respecting
- Never fabricate data — anything without a reliable source must be labeled "unknown / verify manually," not guessed.
- API keys (title search, crime data, etc.) are server-side env vars only, never entered through the UI or exposed client-side, never invented.
- This tool assists research; it does not replace a real title search or attorney review — surface uncertainty rather than hiding it.
- If a county's portal can't be scraped, don't drop it silently — log the failure and keep a link-out to the source in the UI.
